name: Build and Push Docker Image to Docker Hub

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up variables
        id: vars
        run: |
          echo "SHA_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          echo "DATE=$(date +'%Y%m%d')" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image with multiple tags
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/miapp:latest \
                       -t ${{ secrets.DOCKER_USERNAME }}/miapp:${{ env.SHA_SHORT }} \
                       -t ${{ secrets.DOCKER_USERNAME }}/miapp:${{ env.DATE }} .

      - name: Push Docker image (all tags)
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/miapp:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/miapp:${{ env.SHA_SHORT }}
          docker push ${{ secrets.DOCKER_USERNAME }}/miapp:${{ env.DATE }}
